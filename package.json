{
  "name": "conan-exiles-admin-map",
  "version": "0.0.1",
  "description": "A Conan Exiles Admin Dashboard",
  "main": "index.js",
  "scripts": {
    "start": "node src/index.js",
    "build": "babel src --out-dir lib && cp -rf src/views lib/views && pkg lib/index.js -t latest-win-x64 --out-path build -c package.json && cp src/bindings/win-x64/node_sqlite3.node build && rm -rf lib",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Germán Robledo Curiel <germix@germix.net>",
  "license": "MIT",
  "dependencies": {
    "@babel/polyfill": "^7.0.0",
    "ejs": "^2.6.1",
    "express": "^4.16.4",
    "smart-buffer": "^4.0.1",
    "sqlite": "^3.0.0"
  },
  "devDependencies": {
    "@babel/cli": "^7.1.2",
    "@babel/core": "^7.1.2",
    "@babel/preset-env": "^7.1.0",
    "pkg": "^4.3.4"
  },
  "pkg": {
    "assets": [
      "lib/views/**/*",
      "public/**/*"
    ]
  }
}
